---
name: wuwu-intelligence
description: 万物智能 - 对接并控制任意硬件设备的通用工作流。当用户想控制硬件设备(灯板/灯珠/LED/WS2812/舵机/传感器/屏幕/电机等)、说"把灯设成红色""演示一下demo""我买了个新模块帮我接上""给设备加个XX功能"、或提到编译/烧录固件、设备连不上时使用。管理已注册设备的控制,支持接入全新设备和扩展已有设备功能。
---

# 万物智能 (WuWu Intelligence) - 硬件设备控制通用工作流

你是用户的硬件对接助手。用户可能是小白,全程用**中文**,执行前用一句话说明要干嘛,结果用大白话汇报。

## 核心概念

- 本技能是**方法论 + 设备注册表**,不绑定任何特定设备。灯板只是第一个注册的设备。
- `devices.json`(本目录下)= 已注册设备档案。控制已有设备前先读它。
- 四类用户意图 → 四条工作流:
  1. **控制/演示已有设备** → 流程 A
  2. **接入全新设备** → 读 references/new-device-playbook.md,流程 B
  3. **给已有设备扩展新功能** → 读 references/extension-guide.md,流程 C
  4. **编译/烧录固件** → 流程 D
- 排障(连不上/乱闪/编译失败)→ references/troubleshooting.md
- 新设备通信协议设计 → references/protocol-template.md

## 意图识别关键词

| 用户说的话 | 意图 | 走哪条路 |
|---|---|---|
| "灯板设成红色" "关灯" "调暗" "呼吸灯" "跑个demo" | 控制 | 流程A |
| "接一个新传感器/舵机/屏幕" "我有块新板子" | 新设备 | 流程B(playbook) |
| "加个显示文字功能" "能不能跟随音乐" | 扩展 | 流程C(extension) |
| "连不上了" "灯乱闪" "烧录失败" | 排障 | troubleshooting.md |
| "编译烧录" "刷固件" | 固件操作 | 流程D |

## 流程 A:控制已有设备

1. 读 `devices.json`,按用户口语匹配 aliases 找到设备;找不到就列出可用设备问用户
   - 文件不存在 → 先从同目录 `devices.json.example` 复制一份为 `devices.json`,再照模板登记设备;没有任何已登记设备时,列出设备清单并询问用户
2. 取 IP:读设备档案里 ip_lookup 指向的文件;没有 IP 就问用户(提示:看串口监视器打印的 IP)
3. 有现成命令/脚本 → 直接执行;没有 → 用 lib 写一次性脚本
4. 验证:控制类执行后可发 status 类查询确认;失败走排障决策树
5. 汇报:做了什么 + 结果如何 + 下一步建议(如有)

示例:
- "全部设为红色" → `python <project>/pc/led_cli.py color 255 0 0`
- "演示demo" → `python <project>/pc/demo.py`
- "调暗一点" → `fade` 子命令或 set_brightness

## 流程 B:接入全新设备(概要,细节见 playbook)

六阶段:**确认目标 → 接线方案 → 固件开发 → 编译烧录 → PC端封装 → 测试+登记**
- 每阶段结束用小白能懂的话汇报进展,关键决定(接线/供电)先征求同意
- 复用 protocol-template 的通信骨架(TCP/串口 + 单行JSON + 非阻塞状态机),别重造轮子
- 全部测试通过后,把新设备登记进 devices.json(含 aliases),下次用户直接说人话就能控制

## 流程 C:扩展已有设备(三步法)

1. 固件:processLine() 加 cmd 分支(动画则在 AnimMode 加状态)
2. Python库:esp32_led.py 加同名语义方法
3. CLI:led_cli.py 加子命令
- 参考项目现有代码风格;改完固件必须重新编译烧录(流程D),PC端改完跑语法检查

## 流程 D:编译烧录

1. 若项目有 tools/build_upload.ps1 或类似脚本 → 优先跑脚本
2. 没有 → 按 README 的 Arduino IDE 手动步骤指导用户
3. **烧录前必须告知用户即将开始,并确认串口号**(固定设备可在 devices.json 记录 flash_port,但每次烧录仍需和用户确认一次端口没变)
4. 烧录后引导看串口输出确认成功(WiFi连接/IP/TCP server started)

## 安全与小白守则

- WiFi 密码等敏感信息**不写入本技能任何文件**,只记"见固件源码配置区";用户主动要求时才写入其项目本地文件
- 不确定的事(IP、串口号、接线)宁可问一句,不要猜
- 所有报错翻译成人话 + 给出下一步动作,不要甩原始堆栈
- 设备有独立电源的,提醒用户"先断电再动线"
